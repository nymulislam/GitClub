# 🌟 Welcome To GitClub Issues Tracker!

    - 1️⃣ What is the difference between var, let, and const?
    - Ans: All are differences in scop, redeclaration and hoisting, vai is maintained global scop, redeclaration and hoisting, let and const are block scope, but let accept reassigning, while const is not.

    - 2️⃣ What is the spread operator (...)?
    - Ans: Spread operator (...) expands an array or object into individual elements.
  
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    -Ans: map() run loops in array, filter() run function gather items in an array by   conditionally, and forEach() function run loop for every individual items.

    - 4️⃣ What is an arrow function?
    - Ans: arrow function is short version of regular function.
  
    - 5️⃣ What are template literals?
    - Ans: Template literals are modern way to declare string in JS file.




